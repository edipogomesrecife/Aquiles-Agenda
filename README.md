# Projeto AURA / Aquiles

Integração com a API do Google Gemini em Python.

## Como executar
1. Instale as dependências: `pip install google-genai python-dotenv`
2. Configure sua chave: `set GEMINI_API_KEY=sua_chave`
3. Execute o script: `python main.py`
